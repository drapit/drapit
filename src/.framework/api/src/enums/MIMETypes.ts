// TODO: move this to server directory.

/**
 * MIME types enum.
 *
 * @enum {number}
 */
enum MIMETypes {
  json = "application/json",
  xml = "application/xml",
  pdf = "application/pdf",
  formData = "multipart/form-data",
  stream = "application/octet-stream",
  text = "text/plain",
}

export default MIMETypes;
