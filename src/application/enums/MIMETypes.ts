// TODO: move this to server directory.

/**
 * MIME types enum.
 *
 * @enum {number}
 */
enum MIMETypes {
  json = "application/json",
  xml = "application/xml",
  pdf = "application/pdf",
}

export default MIMETypes;
