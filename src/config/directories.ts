export const root = `${process.env.ROOT_DIR}`;
export const swagger = `${root}/.swagger`;
export const api = `${root}/src/api`;
export const application = `${root}/src/application`;
export const global = `${root}/src/global`;
export const infrastructure = `${root}/src/infrastructure`;
